{
  "name": "ts-swagger-generator",
  "version": "0.1.1",
  "author": "martelka",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rm -rf dist/ && npx tsup src --format cjs,esm --dts --clean",
    "compile": "rm -rf dist/ && npx tsc"
  },
  "devDependencies": {
    "tsup": "^6.7.0",
    "typescript": "^4.9.5"
  }
}
